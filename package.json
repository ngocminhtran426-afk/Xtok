{
  "name": "xtok",
  "version": "1.0.0",
  "description": "Video Streaming Platform",
  "scripts": {
    "install": "npm --prefix crawler install && npm --prefix web install",
    "build": "npm --prefix web run build",
    "start": "npm --prefix crawler run api"
  },
  "author": "",
  "license": "ISC"
}
